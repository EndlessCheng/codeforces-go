## 一、寻找子问题

在示例 4 中，我们要解决的问题（原问题）是：

- 有 $n=7$ 个石子，先手（Alice）能否必胜？

枚举先手拿了多少个石子（个数必须是 $[1,n]$ 内的平方数）：

- 拿 $1$ 个石子，问题变成：有 $n'=7-1=6$ 个石子，先手（此时 Bob 为先手）能否必胜？
- 拿 $4$ 个石子，问题变成：有 $n'=7-4=3$ 个石子，先手（此时 Bob 为先手）能否必胜？

这些问题都是**和原问题相似的、规模更小的子问题**，可以用**递归**解决。

## 二、状态定义与状态转移方程

根据上面的讨论，定义 $\textit{dfs}(i)$ 表示在有 $i$ 个石子的情况下，先手能否必胜。

枚举先手拿了 $x^2$ 个石子，其中 $1\le x^2\le i$，问题变成在有 $i-x^2$ 个石子的情况下，先手能否必胜，即 $\textit{dfs}(i-x^2)$。

只要有一个 $\textit{dfs}(i-x^2)$ 是 $\texttt{false}$（对手一定输），那么先手必胜，$\textit{dfs}(i)$ 是 $\texttt{true}$，即

$$
\textit{dfs}(i) = \bigvee_{1\le x^2\le i} \neg \textit{dfs}(i-x^2)
$$

**递归边界**：$\textit{dfs}(0)=\texttt{false}$。没有石子，先手无法操作，先手必败。

**递归入口**：$\textit{dfs}(n)$，这是原问题，也是答案。

## 三、递归搜索 + 保存递归返回值 = 记忆化搜索

考虑到整个递归过程中有大量重复递归调用（递归入参相同）。由于递归函数没有副作用，同样的入参无论计算多少次，算出来的结果都是一样的，因此可以用**记忆化搜索**来优化：

- 如果一个状态（递归入参）是第一次遇到，那么可以在返回前，把状态及其结果记到一个 $\textit{memo}$ 数组中。
- 如果一个状态不是第一次遇到（$\textit{memo}$ 中保存的结果不等于 $\textit{memo}$ 的初始值），那么可以直接返回 $\textit{memo}$ 中保存的结果。

⚠**注意**：$\textit{memo}$ 数组的**初始值**一定不能等于要记忆化的值！例如初始值设置为 $0$，并且要记忆化的 $\textit{dfs}(i)$ 也等于 $0$（$\texttt{false}$），那就没法判断 $0$ 到底表示第一次遇到这个状态，还是表示之前遇到过了，从而导致记忆化失效。一般把初始值设置为 $-1$。

> Python 用户可以无视上面这段，直接用 `@cache` 装饰器。

关于记忆化搜索的原理，请看视频讲解 [动态规划入门：从记忆化搜索到递推【基础算法精讲 17】](https://www.bilibili.com/video/BV1Xj411K7oF/)，其中包含把记忆化搜索 1:1 翻译成递推的技巧。

```py [sol-Python3]
# 把 dfs 写在外面，记忆化的保存的值可以用于后续测试数据
@cache  # 缓存装饰器，避免重复计算 dfs（一行代码实现记忆化）
def dfs(i: int) -> int:
    if i == 0:
        return False
    return any(not dfs(i - x * x) for x in range(1, isqrt(i) + 1))

class Solution:
    def winnerSquareGame(self, n: int) -> bool:
        return dfs(n)
```

```java [sol-Java]
class Solution {
    public boolean winnerSquareGame(int n) {
        int[] memo = new int[n + 1];
        Arrays.fill(memo, -1); // -1 表示该状态没有计算过
        return dfs(n, memo);
    }

    private boolean dfs(int i, int[] memo) {
        if (i == 0) {
            return false;
        }

        if (memo[i] != -1) { // 之前计算过
            return memo[i] == 1;
        }

        for (int x = 1; x * x <= i; x++) {
            if (!dfs(i - x * x, memo)) {
                memo[i] = 1; // 记忆化
                return true;
            }
        }
        memo[i] = 0; // 记忆化
        return false;
    }
}
```

```cpp [sol-C++]
class Solution {
public:
    bool winnerSquareGame(int n) {
        vector<int8_t> memo(n + 1, -1); // -1 表示该状态没有计算过

        auto dfs = [&](this auto&& dfs, int i) -> bool {
            if (i == 0) {
                return false;
            }

            if (memo[i] != -1) { // 之前计算过
                return memo[i];
            }

            for (int x = 1; x * x <= i; x++) {
                if (!dfs(i - x * x)) {
                    return memo[i] = true;
                }
            }
            return memo[i] = false;
        };

        return dfs(n);
    }
};
```

```go [sol-Go]
func winnerSquareGame(n int) bool {
	memo := make([]int8, n+1)
	for i := range memo {
		memo[i] = -1 // -1 表示该状态没有计算过
	}

	var dfs func(int) bool
	dfs = func(i int) bool {
		if i == 0 {
			return false
		}

		p := &memo[i]
		if *p != -1 { // 之前计算过
			return *p == 1
		}

		for x := 1; x*x <= i; x++ {
			if !dfs(i - x*x) {
				*p = 1
				return true
			}
		}
		*p = 0
		return false
	}

	return dfs(n)
}
```

#### 复杂度分析

- 时间复杂度：$\mathcal{O}(n\sqrt n)$。由于每个状态只会计算一次，记忆化搜索的时间复杂度 $=$ 状态个数 $\times$ 单个状态的计算时间。本题状态个数为 $\mathcal{O}(n)$，单个状态的计算时间为 $\mathcal{O}(\sqrt n)$，所以总的时间复杂度为 $\mathcal{O}(n\sqrt n)$。
- 空间复杂度：$\mathcal{O}(n)$。保存多少状态，就需要多少空间。

## 四、1:1 翻译成递推

我们可以去掉递归中的「递」，只保留「归」的部分，即自底向上计算。

具体来说，$f[i]$ 的定义和 $\textit{dfs}(i)$ 的定义是完全一样的，都表示在有 $i$ 个石子的情况下，先手能否必胜。

相应的递推式（状态转移方程）也和 $\textit{dfs}$ 完全一样：

$$
f[i] = \bigvee_{1\le x^2\le i} \neg f[i-x^2]
$$

初始值 $f[0]=\texttt{false}$，翻译自递归边界 $\textit{dfs}(0)=\texttt{false}$。

答案为 $f[n]$，翻译自递归入口 $\textit{dfs}(n)$。

```py [sol-Python3]
class Solution:
    def winnerSquareGame(self, n: int) -> bool:
        # 也可以把 f 写在 class 外面，见【Python3 写法二】
        f = [False] * (n + 1)
        for i in range(1, n + 1):
            f[i] = any(not f[i - x * x] for x in range(1, isqrt(i) + 1))
        return f[n]
```

```py [sol-Python3 写法二]
f = [False] * 100_001
for i in range(1, len(f)):
    f[i] = any(not f[i - x * x] for x in range(1, isqrt(i) + 1))

class Solution:
    def winnerSquareGame(self, n: int) -> bool:
        return f[n]
```

```java [sol-Java]
class Solution {
    public boolean winnerSquareGame(int n) {
        boolean[] f = new boolean[n + 1];
        for (int i = 1; i <= n; i++) {
            for (int x = 1; x * x <= i; x++) {
                if (!f[i - x * x]) {
                    f[i] = true;
                    break;
                }
            }
        }
        return f[n];
    }
}
```

```cpp [sol-C++]
class Solution {
public:
    bool winnerSquareGame(int n) {
        vector<int8_t> f(n + 1);
        for (int i = 1; i <= n; i++) {
            for (int x = 1; x * x <= i; x++) {
                if (!f[i - x * x]) {
                    f[i] = true;
                    break;
                }
            }
        }
        return f[n];
    }
};
```

```go [sol-Go]
func winnerSquareGame(n int) bool {
	f := make([]bool, n+1)
	for i := 1; i <= n; i++ {
		for x := 1; x*x <= i; x++ {
			if !f[i-x*x] {
				f[i] = true
				break
			}
		}
	}
	return f[n]
}
```

#### 复杂度分析

- 时间复杂度：$\mathcal{O}(n\sqrt n)$。
- 空间复杂度：$\mathcal{O}(n)$。

## 五、刷表法（用当前状态更新其它状态）

由状态转移方程可知，如果有 $i$ 个石子时先手必败，那么有 $i+x^2$ 个石子时，先手拿出 $x^2$ 个石子，剩下 $i$ 个石子，对手必败，先手必胜。

据此，如果 $f[i] = \texttt{false}$，那么枚举 $x^2$，更新 $f[i+x^2]$ 也为 $\texttt{true}$，其中 $i+x^2\le n$。

对于 $f[i] = \texttt{true}$ 的状态，由于不影响后续状态，直接 $\texttt{continue}$。

初始值：所有 $f[i]$ 均为 $\texttt{false}$。

```py [sol-Python3]
class Solution:
    def winnerSquareGame(self, n: int) -> bool:
        f = [False] * (n + 1)
        for i in range(n):
            if f[i]:
                continue
            for x in range(1, isqrt(n - i) + 1):
                f[i + x * x] = True
        return f[n]
```

```java [sol-Java]
class Solution {
    public boolean winnerSquareGame(int n) {
        boolean[] f = new boolean[n + 1];
        for (int i = 0; i < n; i++) {
            if (f[i]) {
                continue;
            }
            for (int x = 1; x * x <= n - i; x++) {
                f[i + x * x] = true;
            }
        }
        return f[n];
    }
}
```

```cpp [sol-C++]
class Solution {
public:
    bool winnerSquareGame(int n) {
        vector<int8_t> f(n + 1);
        for (int i = 0; i < n; i++) {
            if (f[i]) {
                continue;
            }
            for (int x = 1; x * x <= n - i; x++) {
                f[i + x * x] = true;
            }
        }
        return f[n];
    }
};
```

```go [sol-Go]
func winnerSquareGame(n int) bool {
	f := make([]bool, n+1)
	for i := range n {
		if f[i] {
			continue
		}
		for x := 1; x*x <= n-i; x++ {
			f[i+x*x] = true
		}
	}
	return f[n]
}
```

#### 复杂度分析

- 时间复杂度：$\mathcal{O}(n\sqrt n)$。在 $[0,10^5]$ 中只有 $2781$ 个 $f[i] = \texttt{false}$（必败态），占比仅 $2.8\%$，所以刷表法比前一种写法（查表法）快很多。
- 空间复杂度：$\mathcal{O}(n)$。

## 专题训练

见下面动态规划题单的「**十四、博弈 DP**」。

## 分类题单

[如何科学刷题？](https://leetcode.cn/discuss/post/3141566/ru-he-ke-xue-shua-ti-by-endlesscheng-q3yd/)

1. [滑动窗口与双指针（定长/不定长/单序列/双序列/三指针/分组循环）](https://leetcode.cn/discuss/post/3578981/ti-dan-hua-dong-chuang-kou-ding-chang-bu-rzz7/)
2. [二分算法（二分答案/最小化最大值/最大化最小值/第K小）](https://leetcode.cn/discuss/post/3579164/ti-dan-er-fen-suan-fa-er-fen-da-an-zui-x-3rqn/)
3. [单调栈（基础/矩形面积/贡献法/最小字典序）](https://leetcode.cn/discuss/post/3579480/ti-dan-dan-diao-zhan-ju-xing-xi-lie-zi-d-u4hk/)
4. [网格图（DFS/BFS/综合应用）](https://leetcode.cn/discuss/post/3580195/fen-xiang-gun-ti-dan-wang-ge-tu-dfsbfszo-l3pa/)
5. [位运算（基础/性质/拆位/试填/恒等式/思维）](https://leetcode.cn/discuss/post/3580371/fen-xiang-gun-ti-dan-wei-yun-suan-ji-chu-nth4/)
6. [图论算法（DFS/BFS/拓扑排序/基环树/最短路/最小生成树/网络流）](https://leetcode.cn/discuss/post/3581143/fen-xiang-gun-ti-dan-tu-lun-suan-fa-dfsb-qyux/)
7. [动态规划（入门/背包/划分/状态机/区间/状压/数位/数据结构优化/树形/博弈/概率期望）](https://leetcode.cn/discuss/post/3581838/fen-xiang-gun-ti-dan-dong-tai-gui-hua-ru-007o/)
8. [常用数据结构（前缀和/差分/栈/队列/堆/字典树/并查集/树状数组/线段树）](https://leetcode.cn/discuss/post/3583665/fen-xiang-gun-ti-dan-chang-yong-shu-ju-j-bvmv/)
9. [数学算法（数论/组合/概率期望/博弈/计算几何/随机算法）](https://leetcode.cn/discuss/post/3584388/fen-xiang-gun-ti-dan-shu-xue-suan-fa-shu-gcai/)
10. [贪心与思维（基本贪心策略/反悔/区间/字典序/数学/思维/脑筋急转弯/构造）](https://leetcode.cn/discuss/post/3091107/fen-xiang-gun-ti-dan-tan-xin-ji-ben-tan-k58yb/)
11. [链表、树与回溯（前后指针/快慢指针/DFS/BFS/直径/LCA）](https://leetcode.cn/discuss/post/3142882/fen-xiang-gun-ti-dan-lian-biao-er-cha-sh-6srp/)
12. [字符串（KMP/Z函数/Manacher/字符串哈希/AC自动机/后缀数组/子序列自动机）](https://leetcode.cn/discuss/post/3144832/fen-xiang-gun-ti-dan-zi-fu-chuan-kmpzhan-ugt4/)

[我的题解精选（已分类）](https://github.com/EndlessCheng/codeforces-go/blob/master/leetcode/SOLUTIONS.md)

欢迎关注 [B站@灵茶山艾府](https://space.bilibili.com/206214)
