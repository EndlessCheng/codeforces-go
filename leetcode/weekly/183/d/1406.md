## 一、寻找子问题

设 Alice 的最终得分为 $A$，Bob 的最终得分为 $B$。如果 $A > B$，那么 Alice 获胜；如果 $A < B$，那么 Bob 获胜。

$A > B$ 可以变形为 $A-B > 0$，这意味着，我们只需关注得分之差 $A-B$ 的值，而不是 $A$ 和 $B$ 具体是多少。维护一个值，比维护两个值更容易。

用得分之差的观点来看，为了获胜，Alice 需要最大化 $A-B$，Bob 需要最小化 $A-B$（或者说最大化 $B-A$）。

也就是说，每个玩家都需要**最大化自己的得分减去对手的得分**。

为方便描述，下文把 $\textit{stoneValue}$ 简记为 $a$。

看示例 1，$a = [1,2,3,7]$。枚举 Alice 在第一回合移除的整数：

- 移除 $1$，接下来需要解决的问题是：剩余整数为 $a' = [2,3,7]$，Bob（现在是先手）的得分减去 Alice（现在是后手）的得分最大是多少。
- 移除 $1,2$，接下来需要解决的问题是：剩余整数为 $a' = [3,7]$，Bob（现在是先手）的得分减去 Alice（现在是后手）的得分最大是多少。
- 移除 $1,2,3$，接下来需要解决的问题是：剩余整数为 $a' = [7]$，Bob（现在是先手）的得分减去 Alice（现在是后手）的得分最大是多少。

我们需要解决的子问题，都是**最大化先手的得分减去后手的得分**，都是**和原问题相似的、规模更小的子问题**，所以可以用**递归**解决。

虽然我们找到了子问题，但是，**原问题和子问题的关系**是什么？怎么利用子问题的结果，算出原问题的答案？

## 二、状态定义与状态转移方程

因为要解决的问题都形如「对于 $a$ 的后缀，计算先手得分减去后手得分的最大值」，所以定义 $\textit{dfs}(i)$ 表示当剩余整数为 $a$ 的后缀 $[i,n-1]$ 时，先手得分减去后手得分的最大值。

例如 $a = [1,2,3,7]$。在第一回合，如果 Alice 移除 $1,2$，那么 Alice 的得分增加

$$
s = 1 + 2 = 3
$$

子问题为：对于 $a' = [3,7]$，Bob 如何操作，可以最大化「Bob 的得分减去 Alice 的得分」。注意这里的得分都是在 $a'$ 上的得分。

对于 $a'$，设 Bob 的得分为 $B'$，Alice 的得分为 $A'$，那么子问题可以表示为

$$
\textit{dfs}(2) = B'-A'
$$

我们要计算的原问题可以表示为

$$
\textit{dfs}(0)=A-B
$$

二者的关系是什么？

由于 $A = s + A',\ B = B'$，所以有

$$
\textit{dfs}(0) = A-B = (s+A') - B' = s - (B'-A') = s - \textit{dfs}(2)
$$

即

$$
\textit{dfs}(0) = s - \textit{dfs}(2)
$$

这样就找到了**原问题和子问题的关系**。

一般地，如果剩余整数为 $a$ 的子数组 $[i,n-1]$，我们枚举先手移除的整数为子数组 $[i,j]$，其中 $i\le j\le \min(i+2,n-1)$，问题变成当剩余整数为 $a$ 的后缀 $[j+1,n-1]$ 时，先手得分减去后手得分的最大值，即 $\textit{dfs}(j+1)$。

取最大值，得

$$
\textit{dfs}(i) = \max_{j=i}^{\min(i+2,n-1)} \left(\sum_{k=i}^{j}a[k]\right) - \textit{dfs}(j+1)
$$

**递归边界**：$\textit{dfs}(n)=0$。此时没有整数，得分之差为 $0 - 0 = 0$。

**递归入口**：$\textit{dfs}(0)$，这是两人得分之差（先手得分减去后手得分）的最大值。

## 三、递归搜索 + 保存递归返回值 = 记忆化搜索

考虑到整个递归过程中有大量重复递归调用（递归入参相同）。由于递归函数没有副作用，同样的入参无论计算多少次，算出来的结果都是一样的，因此可以用**记忆化搜索**来优化：

- 如果一个状态（递归入参）是第一次遇到，那么可以在返回前，把状态及其结果记到一个 $\textit{memo}$ 数组中。
- 如果一个状态不是第一次遇到（$\textit{memo}$ 中保存的结果不等于 $\textit{memo}$ 的初始值），那么可以直接返回 $\textit{memo}$ 中保存的结果。

⚠**注意**：$\textit{memo}$ 数组的**初始值**一定不能等于要记忆化的值！例如初始值设置为 $0$，并且要记忆化的 $\textit{dfs}(i)$ 也等于 $0$，那就没法判断 $0$ 到底表示第一次遇到这个状态，还是表示之前遇到过了，从而导致记忆化失效。一般把初始值设置为 $-1$，但本题 $\textit{dfs}(i)$ 的返回值可能是负数，可以初始化为 $\infty$ 或 $-\infty$。

> Python 用户可以无视上面这段，直接用 `@cache` 装饰器。

关于记忆化搜索的原理，请看视频讲解 [动态规划入门：从记忆化搜索到递推【基础算法精讲 17】](https://www.bilibili.com/video/BV1Xj411K7oF/)，其中包含把记忆化搜索 1:1 翻译成递推的技巧。

```py [sol-Python3]
class Solution:
    def stoneGameIII(self, stoneValue: List[int]) -> str:
        n = len(stoneValue)

        @cache  # 缓存装饰器，避免重复计算 dfs（一行代码实现记忆化）
        def dfs(i: int) -> int:
            if i == n:
                return 0
            res = -inf
            s = 0
            for j in range(i, min(i + 3, n)):
                s += stoneValue[j]
                res = max(res, s - dfs(j + 1))
            return res

        diff = dfs(0)
        if diff == 0:
            return "Tie"
        return "Alice" if diff > 0 else "Bob"
```

```java [sol-Java]
class Solution {
    public String stoneGameIII(int[] stoneValue) {
        int n = stoneValue.length;
        int[] memo = new int[n];
        Arrays.fill(memo, Integer.MIN_VALUE); // MIN_VALUE 表示该状态没有计算过

        int diff = dfs(0, stoneValue, memo);
        if (diff == 0) {
            return "Tie";
        }
        return diff > 0 ? "Alice" : "Bob";
    }

    private int dfs(int i, int[] stoneValue, int[] memo) {
        if (i == stoneValue.length) {
            return 0;
        }

        if (memo[i] != Integer.MIN_VALUE) { // 之前计算过
            return memo[i];
        }

        int res = Integer.MIN_VALUE;
        int sum = 0;
        for (int j = i; j < Math.min(i + 3, stoneValue.length); j++) {
            sum += stoneValue[j];
            res = Math.max(res, sum - dfs(j + 1, stoneValue, memo));
        }
        return memo[i] = res; // 记忆化
    }
}
```

```cpp [sol-C++]
class Solution {
public:
    string stoneGameIII(vector<int>& stoneValue) {
        int n = stoneValue.size();
        vector<int> memo(n, INT_MIN); // INT_MIN 表示该状态没有计算过

        auto dfs = [&](this auto&& dfs, int i) -> int {
            if (i == n) {
                return 0;
            }

            int& res = memo[i]; // 注意这里是引用
            if (res != INT_MIN) { // 之前计算过
                return res;
            }

            int sum = 0;
            for (int j = i; j < min(i + 3, n); j++) {
                sum += stoneValue[j];
                res = max(res, sum - dfs(j + 1));
            }
            return res;
        };

        int diff = dfs(0);
        if (diff == 0) {
            return "Tie";
        }
        return diff > 0 ? "Alice" : "Bob";
    }
};
```

```go [sol-Go]
func stoneGameIII(stoneValue []int) string {
	n := len(stoneValue)
	memo := make([]int, n)
	for i := range memo {
		memo[i] = math.MinInt // MinInt 表示该状态没有计算过
	}

	var dfs func(int) int
	dfs = func(i int) int {
		if i == n {
			return 0
		}

		p := &memo[i]
		if *p != math.MinInt { // 之前计算过
			// 之前计算过
			return *p
		}

		res := math.MinInt
		sum := 0
		for j := i; j < min(i+3, n); j++ {
			sum += stoneValue[j]
			res = max(res, sum-dfs(j+1))
		}
		*p = res // 记忆化
		return res
	}

	diff := dfs(0)
	if diff == 0 {
		return "Tie"
	}
	if diff > 0 {
		return "Alice"
	}
	return "Bob"
}
```

#### 复杂度分析

- 时间复杂度：$\mathcal{O}(nK)$，其中 $n$ 是 $\textit{stoneValue}$ 的长度，$K=3$ 是每回合玩家可以移除的整数个数。由于每个状态只会计算一次，记忆化搜索的时间复杂度 $=$ 状态个数 $\times$ 单个状态的计算时间。本题状态个数为 $\mathcal{O}(n)$，单个状态的计算时间为 $\mathcal{O}(K)$，所以总的时间复杂度为 $\mathcal{O}(nK)$。
- 空间复杂度：$\mathcal{O}(n)$。保存多少状态，就需要多少空间。

## 四、1:1 翻译成递推

我们可以去掉递归中的「递」，只保留「归」的部分，即自底向上计算。

具体来说，$f[i]$ 的定义和 $\textit{dfs}(i)$ 的定义是完全一样的，都表示当剩余整数为 $a$ 的后缀 $[i,n-1]$ 时，先手得分减去后手得分的最大值。

相应的递推式（状态转移方程）也和 $\textit{dfs}$ 一样：

$$
f[i] = \max_{j=i}^{\min(i+2,n-1)} \left(\sum_{k=i}^{j}a[k]\right) - f[j+1]
$$

初始值 $f[n]=0$，翻译自递归边界 $\textit{dfs}(n)=0$。

两人得分之差（先手得分减去后手得分）的最大值为 $f[0]$，翻译自递归入口 $\textit{dfs}(0)$。

```py [sol-Python3]
class Solution:
    def stoneGameIII(self, stoneValue: List[int]) -> str:
        n = len(stoneValue)
        f = [-inf] * n + [0]
        for i in range(n - 1, -1, -1):
            s = 0
            for j in range(i, min(i + 3, n)):
                s += stoneValue[j]
                f[i] = max(f[i], s - f[j + 1])

        diff = f[0]
        if diff == 0:
            return "Tie"
        return "Alice" if diff > 0 else "Bob"
```

```java [sol-Java]
class Solution {
    public String stoneGameIII(int[] stoneValue) {
        int n = stoneValue.length;
        int[] f = new int[n + 1];

        for (int i = n - 1; i >= 0; i--) {
            f[i] = Integer.MIN_VALUE;
            int sum = 0;
            for (int j = i; j < Math.min(i + 3, n); j++) {
                sum += stoneValue[j];
                f[i] = Math.max(f[i], sum - f[j + 1]);
            }
        }

        int diff = f[0];
        if (diff == 0) {
            return "Tie";
        }
        return diff > 0 ? "Alice" : "Bob";
    }
}
```

```cpp [sol-C++]
class Solution {
public:
    string stoneGameIII(vector<int>& stoneValue) {
        int n = stoneValue.size();
        vector<int> f(n + 1);

        for (int i = n - 1; i >= 0; i--) {
            f[i] = INT_MIN;
            int sum = 0;
            for (int j = i; j < min(i + 3, n); j++) {
                sum += stoneValue[j];
                f[i] = max(f[i], sum - f[j + 1]);
            }
        }

        int diff = f[0];
        if (diff == 0) {
            return "Tie";
        }
        return diff > 0 ? "Alice" : "Bob";
    }
};
```

```go [sol-Go]
func stoneGameIII(stoneValue []int) string {
	n := len(stoneValue)
	f := make([]int, n+1)

	for i := n - 1; i >= 0; i-- {
		f[i] = math.MinInt
		sum := 0
		for j := i; j < min(i+3, n); j++ {
			sum += stoneValue[j]
			f[i] = max(f[i], sum-f[j+1])
		}
	}

	diff := f[0]
	if diff == 0 {
		return "Tie"
	}
	if diff > 0 {
		return "Alice"
	}
	return "Bob"
}
```

#### 复杂度分析

- 时间复杂度：$\mathcal{O}(nK)$，其中 $n$ 是 $\textit{stoneValue}$ 的长度，$K=3$ 是每回合玩家可以移除的整数个数。
- 空间复杂度：$\mathcal{O}(n)$。

## 五、另一种状态定义：最大化自己的得分

自己的得分 + 对手的得分 = 元素总和。

得分之差 = 自己的得分 - 对手的得分 = 自己的得分 - (元素总和 - 自己的得分) = 自己的得分 × 2 - 元素总和。

由于元素总和是固定的，所以自己的得分越大，得分之差越大。

所以，也可以定义 $f[i]$ 表示当剩余整数为 $a$ 的后缀 $[i,n-1]$ 时，先手得分的最大值。

枚举先手选了子数组 $[i,j]$，那么问题变成当剩余整数为 $a$ 的后缀 $[j+1,n-1]$ 时，后手（变成先手）的得分的最大值，即 $f[j+1]$。

由于

$$
先手得分 + 后手得分 = [i,n-1]\ 的元素和
$$

所以

$$
先手得分 = [i,n-1]\ 的元素和 - 后手得分
$$

后手得分越小，先手得分越大，所以有

$$
f[i] = \left(\sum_{j=i}^{n-1}a[j]\right) - \min_{j=i}^{\min(i+2,n-1)} f[j+1]
$$

初始值 $f[n]=0$。

Alice 的最大得分为 $f[0]$。

代码实现时，可以创建 $f[n+1] = f[n+2] = 0$，从而简化 $i=n-1$ 和 $i=n-2$ 时的转移。

```py [sol-Python3]
class Solution:
    def stoneGameIII(self, stoneValue: List[int]) -> str:
        n = len(stoneValue)
        f = [0] * (n + 3)
        suf_sum = 0  # stoneValue 的后缀和
        for i in range(n - 1, -1, -1):
            suf_sum += stoneValue[i]
            f[i] = suf_sum - min(f[i + 1], f[i + 2], f[i + 3])

        diff = f[0] - (suf_sum - f[0])
        if diff == 0:
            return "Tie"
        return "Alice" if diff > 0 else "Bob"
```

```java [sol-Java]
class Solution {
    public String stoneGameIII(int[] stoneValue) {
        int n = stoneValue.length;
        int[] f = new int[n + 3];
        int sufSum = 0; // stoneValue 的后缀和
        for (int i = n - 1; i >= 0; i--) {
            sufSum += stoneValue[i];
            f[i] = sufSum - Math.min(Math.min(f[i + 1], f[i + 2]), f[i + 3]);
        }

        int diff = f[0] - (sufSum - f[0]);
        if (diff == 0) {
            return "Tie";
        }
        return diff > 0 ? "Alice" : "Bob";
    }
}
```

```cpp [sol-C++]
class Solution {
public:
    string stoneGameIII(vector<int>& stoneValue) {
        int n = stoneValue.size();
        vector<int> f(n + 3);
        int suf_sum = 0; // stoneValue 的后缀和
        for (int i = n - 1; i >= 0; i--) {
            suf_sum += stoneValue[i];
            f[i] = suf_sum - min({f[i + 1], f[i + 2], f[i + 3]});
        }

        int diff = f[0] - (suf_sum - f[0]);
        if (diff == 0) {
            return "Tie";
        }
        return diff > 0 ? "Alice" : "Bob";
    }
};
```

```go [sol-Go]
func stoneGameIII(stoneValue []int) string {
	n := len(stoneValue)
	f := make([]int, n+3)
	sufSum := 0 // stoneValue 的后缀和
	for i := n - 1; i >= 0; i-- {
		sufSum += stoneValue[i]
		f[i] = sufSum - min(f[i+1], f[i+2], f[i+3])
	}

	diff := f[0] - (sufSum - f[0])
	if diff == 0 {
		return "Tie"
	}
	if diff > 0 {
		return "Alice"
	}
	return "Bob"
}
```

#### 复杂度分析

- 时间复杂度：$\mathcal{O}(nK)$，其中 $n$ 是 $\textit{stoneValue}$ 的长度，$K=3$ 是每回合玩家可以移除的整数个数。
- 空间复杂度：$\mathcal{O}(n+K)$。

## 六、空间优化

用三个变量 $f_1,f_2,f_3$ 分别表示最近的三个 $f$ 值。

```py [sol-Python3]
class Solution:
    def stoneGameIII(self, stoneValue: List[int]) -> str:
        suf_sum = f1 = f2 = f3 = 0
        for val in reversed(stoneValue):
            suf_sum += val
            f1, f2, f3 = suf_sum - min(f1, f2, f3), f1, f2

        diff = f1 - (suf_sum - f1)
        if diff == 0:
            return "Tie"
        return "Alice" if diff > 0 else "Bob"
```

```java [sol-Java]
class Solution {
    public String stoneGameIII(int[] stoneValue) {
        int sufSum = 0;
        int f1 = 0;
        int f2 = 0;
        int f3 = 0;
        for (int i = stoneValue.length - 1; i >= 0; i--) {
            sufSum += stoneValue[i];
            int newF = sufSum - Math.min(Math.min(f1, f2), f3);
            f3 = f2;
            f2 = f1;
            f1 = newF;
        }

        int diff = f1 - (sufSum - f1);
        if (diff == 0) {
            return "Tie";
        }
        return diff > 0 ? "Alice" : "Bob";
    }
}
```

```cpp [sol-C++]
class Solution {
public:
    string stoneGameIII(vector<int>& stoneValue) {
        int suf_sum = 0, f1 = 0, f2 = 0, f3 = 0;
        for (int i = stoneValue.size() - 1; i >= 0; i--) {
            suf_sum += stoneValue[i];
            int new_f = suf_sum - min(min(f1, f2), f3); // 这样写比 min({f1, f2, f3}) 更快
            f3 = f2;
            f2 = f1;
            f1 = new_f;
        }

        int diff = f1 - (suf_sum - f1);
        if (diff == 0) {
            return "Tie";
        }
        return diff > 0 ? "Alice" : "Bob";
    }
};
```

```go [sol-Go]
func stoneGameIII(stoneValue []int) string {
	var sufSum, f1, f2, f3 int
	for i := len(stoneValue) - 1; i >= 0; i-- {
		sufSum += stoneValue[i]
		f1, f2, f3 = sufSum-min(f1, f2, f3), f1, f2
	}

	diff := f1 - (sufSum - f1)
	if diff == 0 {
		return "Tie"
	}
	if diff > 0 {
		return "Alice"
	}
	return "Bob"
}
```

#### 复杂度分析

- 时间复杂度：$\mathcal{O}(nK)$，其中 $n$ 是 $\textit{stoneValue}$ 的长度，$K=3$ 是每回合玩家可以移除的整数个数。
- 空间复杂度：$\mathcal{O}(K)$。

## 七、单调队列优化

转移方程中的

$$
\min_{j=i}^{\min(i+2,n-1)} f[j+1]
$$

本质是在计算长为 $3$ 的**滑动窗口最小值**。做法同 [239. 滑动窗口最大值](https://leetcode.cn/problems/sliding-window-maximum/)，原理请看 [单调队列【基础算法精讲 27】](https://www.bilibili.com/video/BV1bM411X72E/)。

#### 答疑

**问**：为什么这里的三步骤与 239 题不同？

**答**：对于 239 题，$\textit{nums}[i]$ **在**要计算最大值的窗口中，所以要先把 $\textit{nums}[i]$ 放入窗口。而本题 $f[i]$ **不在**要计算最小值的窗口中，所以在计算转移之后，才把 $f[i]$ 放入窗口。

```py [sol-Python3]
class Solution:
    def stoneGameIII(self, stoneValue: List[int]) -> str:
        K = 3
        n = len(stoneValue)
        f = [0] * (n + 1)
        q = deque([n])
        suf_sum = 0  # stoneValue 的后缀和

        for i in range(n - 1, -1, -1):
            suf_sum += stoneValue[i]

            # 窗口向左滑动
            # 1. 右边出，保证窗口元素下标在 [i+1, i+K] 中
            if q[-1] > i + K:  # 离开窗口
                q.pop()

            # 2. 计算转移
            f[i] = suf_sum - f[q[-1]]

            # 3. 左边入，先去掉无用数据，再把 i 入队
            while q and f[q[0]] >= f[i]:  # f[i] 更小（或者相等）
                q.popleft()
            q.appendleft(i)

        diff = f[0] * 2 - suf_sum
        if diff == 0:
            return "Tie"
        return "Alice" if diff > 0 else "Bob"
```

```java [sol-Java]
class Solution {
    public String stoneGameIII(int[] stoneValue) {
        final int K = 3;
        int n = stoneValue.length;
        int[] f = new int[n + 1];
        Deque<Integer> q = new ArrayDeque<>();
        q.offerLast(n);
        int sufSum = 0; // stoneValue 的后缀和

        for (int i = n - 1; i >= 0; i--) {
            sufSum += stoneValue[i];

            // 窗口向左滑动
            // 1. 右边出，保证窗口元素下标在 [i+1, i+K] 中
            if (q.peekLast() > i + K) { // 离开窗口
                q.pollLast();
            }

            // 2. 计算转移
            f[i] = sufSum - f[q.peekLast()];

            // 3. 左边入，先去掉无用数据，再把 i 入队
            while (!q.isEmpty() && f[q.peekFirst()] >= f[i]) { // f[i] 更小（或者相等）
                q.pollFirst();
            }
            q.offerFirst(i);
        }

        int diff = f[0] * 2 - sufSum;
        if (diff == 0) {
            return "Tie";
        }
        return diff > 0 ? "Alice" : "Bob";
    }
}
```

```cpp [sol-C++]
class Solution {
public:
    string stoneGameIII(vector<int>& stoneValue) {
        constexpr int K = 3;
        int n = stoneValue.size();
        vector<int> f(n + 1);
        deque<int> q = {n};
        int suf_sum = 0; // stoneValue 的后缀和

        for (int i = n - 1; i >= 0; i--) {
            suf_sum += stoneValue[i];

            // 窗口向左滑动
            // 1. 右边出，保证窗口元素下标在 [i+1, i+K] 中
            if (q.back() > i + K) { // 离开窗口
                q.pop_back();
            }

            // 2. 计算转移
            f[i] = suf_sum - f[q.back()];

            // 3. 左边入，先去掉无用数据，再把 i 入队
            while (!q.empty() && f[q.front()] >= f[i]) { // f[i] 更小（或者相等）
                q.pop_front();
            }
            q.push_front(i);
        }

        int diff = f[0] * 2 - suf_sum;
        if (diff == 0) {
            return "Tie";
        }
        return diff > 0 ? "Alice" : "Bob";
    }
};
```

```go [sol-Go]
func stoneGameIII(stoneValue []int) string {
	const k = 3
	n := len(stoneValue)
	f := make([]int, n+1)
	q := []int{n}
	sufSum := 0 // stoneValue 的后缀和

	for i := n - 1; i >= 0; i-- {
		sufSum += stoneValue[i]

		// 窗口向左滑动
		// 1. 右边出，保证窗口元素下标在 [i+1, i+k] 中
		if q[0] > i+k { // 离开窗口
			q = q[1:]
		}

		// 2. 计算转移
		f[i] = sufSum - f[q[0]]

		// 3. 左边入，先去掉无用数据，再把 i 入队
		for len(q) > 0 && f[q[len(q)-1]] >= f[i] { // f[i] 更小（或者相等）
			q = q[:len(q)-1]
		}
		q = append(q, i)
	}

	diff := f[0]*2 - sufSum
	if diff == 0 {
		return "Tie"
	}
	if diff > 0 {
		return "Alice"
	}
	return "Bob"
}
```

#### 复杂度分析

- 时间复杂度：$\mathcal{O}(n)$，其中 $n$ 是 $\textit{stoneValue}$ 的长度。时空复杂度均与 $K$ 无关。
- 空间复杂度：$\mathcal{O}(n)$。

## 专题训练

见下面动态规划题单的「**十四、博弈 DP**」和「**§11.3 单调队列优化 DP**」。

## 分类题单

[如何科学刷题？](https://leetcode.cn/discuss/post/3141566/ru-he-ke-xue-shua-ti-by-endlesscheng-q3yd/)

1. [滑动窗口与双指针（定长/不定长/单序列/双序列/三指针/分组循环）](https://leetcode.cn/discuss/post/3578981/ti-dan-hua-dong-chuang-kou-ding-chang-bu-rzz7/)
2. [二分算法（二分答案/最小化最大值/最大化最小值/第K小）](https://leetcode.cn/discuss/post/3579164/ti-dan-er-fen-suan-fa-er-fen-da-an-zui-x-3rqn/)
3. [单调栈（基础/矩形面积/贡献法/最小字典序）](https://leetcode.cn/discuss/post/3579480/ti-dan-dan-diao-zhan-ju-xing-xi-lie-zi-d-u4hk/)
4. [网格图（DFS/BFS/综合应用）](https://leetcode.cn/discuss/post/3580195/fen-xiang-gun-ti-dan-wang-ge-tu-dfsbfszo-l3pa/)
5. [位运算（基础/性质/拆位/试填/恒等式/思维）](https://leetcode.cn/discuss/post/3580371/fen-xiang-gun-ti-dan-wei-yun-suan-ji-chu-nth4/)
6. [图论算法（DFS/BFS/拓扑排序/基环树/最短路/最小生成树/网络流）](https://leetcode.cn/discuss/post/3581143/fen-xiang-gun-ti-dan-tu-lun-suan-fa-dfsb-qyux/)
7. [动态规划（入门/背包/划分/状态机/区间/状压/数位/数据结构优化/树形/博弈/概率期望）](https://leetcode.cn/discuss/post/3581838/fen-xiang-gun-ti-dan-dong-tai-gui-hua-ru-007o/)
8. [常用数据结构（前缀和/差分/栈/队列/堆/字典树/并查集/树状数组/线段树）](https://leetcode.cn/discuss/post/3583665/fen-xiang-gun-ti-dan-chang-yong-shu-ju-j-bvmv/)
9. [数学算法（数论/组合/概率期望/博弈/计算几何/随机算法）](https://leetcode.cn/discuss/post/3584388/fen-xiang-gun-ti-dan-shu-xue-suan-fa-shu-gcai/)
10. [贪心与思维（基本贪心策略/反悔/区间/字典序/数学/思维/脑筋急转弯/构造）](https://leetcode.cn/discuss/post/3091107/fen-xiang-gun-ti-dan-tan-xin-ji-ben-tan-k58yb/)
11. [链表、树与回溯（前后指针/快慢指针/DFS/BFS/直径/LCA）](https://leetcode.cn/discuss/post/3142882/fen-xiang-gun-ti-dan-lian-biao-er-cha-sh-6srp/)
12. [字符串（KMP/Z函数/Manacher/字符串哈希/AC自动机/后缀数组/子序列自动机）](https://leetcode.cn/discuss/post/3144832/fen-xiang-gun-ti-dan-zi-fu-chuan-kmpzhan-ugt4/)

[我的题解精选（已分类）](https://github.com/EndlessCheng/codeforces-go/blob/master/leetcode/SOLUTIONS.md)

欢迎关注 [B站@灵茶山艾府](https://space.bilibili.com/206214)
